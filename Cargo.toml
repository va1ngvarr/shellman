[package]
name = "shellman"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
comfy-table = "6"
regex = "1.12.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

